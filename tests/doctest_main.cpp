/*
 * this file is pretty much empty because, we just need to have only a SINGLE
 * instance of 'DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN' in our tests, which is what
 * this provides
 **/

/// main(...) is provided by doctest
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest/doctest.h"
